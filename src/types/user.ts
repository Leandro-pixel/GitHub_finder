export type UserProps = { //para não precisarmos tipar em todas as páginas que formos usar
    avatar_url: string;
    login: string;
    location: string;
    followers: number;
    following: number;
};